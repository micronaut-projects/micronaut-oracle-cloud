plugins {
    id 'io.micronaut.build.internal.oraclecloud-module'
}

dependencies {
    implementation(mn.netty.codec.http)
    implementation(libs.oci.common)
    api(libs.oci.common.httpclient)
    api mn.micronaut.serde.api
    implementation mn.micronaut.serde.support

    testImplementation(mn.micronaut.http.server.netty)
    // for self-signed certs
    testImplementation("org.bouncycastle:bcpkix-jdk15on:1.70")
    testImplementation(projects.micronautOraclecloudSdk) {
        exclude group: 'com.oracle.oci.sdk'
    }

    testAnnotationProcessor mn.micronaut.inject.java
    testAnnotationProcessor mn.micronaut.serde.processor
    testAnnotationProcessor projects.oraclecloudHttpclientNettyProcessor

    testImplementation mn.micronaut.inject.java.test
    testImplementation mn.micronaut.serde.api
    testImplementation mn.micronaut.serde.processor
    testImplementation mn.micronaut.serde.jackson
    testImplementation projects.oraclecloudHttpclientNettyProcessor
    testImplementation mn.micronaut.management

    testImplementation (projects.oraclecloudBmcMonitoring) {
        transitive = false
    }
}
tasks.withType(Test).configureEach {
    useJUnitPlatform()
}
