# Every request through the embedded Fn test server starts a new function classloader with its own
# application context and GraalPy runtime (interpreted), which takes a while
micronaut.http.client.read-timeout=180s
