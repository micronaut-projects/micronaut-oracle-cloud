plugins {
    id 'io.micronaut.build.internal.oraclecloud-module'
}

dependencies {
    api(mn.micronaut.inject)
    api(mn.micronaut.runtime)
    api(mnMicrometer.micrometer.core)
    api(mnMicrometer.micronaut.micrometer.core)
    api(projects.micronautOraclecloudBmcMonitoring)
    implementation(mn.micronaut.http.client)
    testImplementation(mn.micronaut.management)
    testImplementation(mnTest.micronaut.test.spock)
    testImplementation(projects.micronautOraclecloudHttpclientNetty)
    testImplementation(projects.micronautOraclecloudSdk)
    testRuntimeOnly(mn.micronaut.http.server.netty)
}
